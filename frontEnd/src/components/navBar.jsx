import React from "react";
import "../styles/styles.css";

const NavBar=() =>{
    return(
        <nav>
            
            <a href="#home">Home</a>
            <a href="#shop">Shop</a>
            <a href="#contact">Contacto</a>
        </nav>
    )
}

export default NavBar;